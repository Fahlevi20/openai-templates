#OpenAI Templates

```bash
python -m venv .venv

.venv/Scripts/Activate

pip install poetry
poetry init
or 
poetry init -n
poetry install

```

```bash
python -m venv .venv
source .venv/bin/activate
pip install poetry
poetry init
or 
poetry init -n

poetry install
```

